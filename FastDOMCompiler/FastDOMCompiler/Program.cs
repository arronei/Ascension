namespace MS.Internal
{
    /// <summary>Root class for the application</summary>
    public class FastDomCompiler
    {
        /// <summary>Main method for the application, and app entry point</summary>
        /// <param name="args">Input arguments for the app</param>
        public static void Main(string[] args)
        {
            var inputWebIdlLocaiton = string.Empty;

            var outputDirectory = string.Empty;
            var hdlDirectory = string.Empty;
            var validateDirectory = string.Empty;
            var buildEnvironment = string.Empty;
        }
    }
}